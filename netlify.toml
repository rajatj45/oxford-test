# netlify.toml
[build]
  command = "next build"
  publish = ".next"        # Keep this; the plugin will handle functions/edge output.

# Enable the Next.js Runtime
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"